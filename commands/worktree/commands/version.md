# Command: version

## Usage

```
version
```

Display the worktree skill version number.

## Implementation

Read the version from `commands/worktree/version.txt` and output:

```
Worktree v{version}
```

## Example

```
Worktree v1.0.0
```
